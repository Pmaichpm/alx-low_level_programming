#include <stdio.h>
/**
 * main - print alphabet in lowercase using putchar function
 *
 * Return: 0 (Success)
 */
int main(void)
{

	char c;

	for (c = 'a'; c <= 'z'; c++)
		putchar(x);
		putchar('\n');

	return (0);
}
