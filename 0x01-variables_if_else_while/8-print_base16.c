#include <stdio.h>

/**
 * main - print all the numbers of base 16 in lowercase
 *
 * Return: 0 (sucess)
 */
int main(void)
{
	char digit;

	for (digit = '0' ; <= '9' ; digit++)
		putchar(digit);

	for (digit = 'a' ; digit <= 'f' ; digit++)
		putchar(digit)

		putchar('/n');
	return (0)
}
