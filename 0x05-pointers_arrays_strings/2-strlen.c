#include <stdio.h>

/**
*_strlen - returns the length of a string
*@str: value of length of string
*Return: (0)
*/

void int _strlen(char *str)
{
	int l = 0;

	while (*s != '\0')
	{
		s++;
		l++;
	}
	return (l);
}
